apiVersion: apps/v1
kind: Deployment
metadata:
  name: g3-consumer
  namespace: namespace-g3
spec:
  selector:
    matchLabels:
      app: g3-consumer
  template:
    metadata:
      labels:
        app: g3-consumer
    spec:
      containers:
        - name: g3-consumer
          image: $URL_IMG_CONSUMER_DOCKER
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 8083
